
struct bowler {
  int bowlingBalls;
};
struct Player {
  bool dead;
};

int y;
int j;
bool shoes(bool color){
	bool blue;
	blue = color;
	if(blue){
		return true;
	}
	return false;
}
int numShoes(int num){
	int total;
	int surplus;
	surplus = 2*num + 10;
	total = surplus - num;
	return total;
}
void getShoes(int num){
	bool color;
	bool colorShoes;
	int numberShoes;
	color = false;
	numberShoes = numShoes(num);
	colorShoes = shoes(color);
	color = (colorShoes = false);
	return;
}

	
int jake(){
  bool start;
  int e;
  int i;
  int g;
  bool b;
  struct bowler bowlerHatMan;
  struct Player Ibra;
  start = false;
  start = true;
  i = 0;
  g = 1;
  
  e = (i + g);
  e = (i - g);
  e = (i * g);
  e = (i / g);
  g++;
  g--;
  
  e = (i > g);
  e = (i < g);
  e = (i >= g);
  e = (i <= g);
  
  
  b = (!start);
  b = (true && start);
  b = (false || start);
  if(start){}
  while(start){}
  repeat(i){}
  if(start == true){}
  b = true;
  b = shoes(start);
  
  bowlerHatMan.bowlingBalls = 3;
  Ibra.dead = false;
  e = (i + bowlerHatMan.bowlingBalls);
  b = (true && Ibra.dead);
}
